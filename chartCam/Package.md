# Package io.healthplatform.chartcam

Provides the core application entry points, platform-specific initializers, and top-level definitions for the ChartCam application. This package contains the `App` composable which serves as the root of the UI hierarchy and orchestrates the dependency graph and routing.
